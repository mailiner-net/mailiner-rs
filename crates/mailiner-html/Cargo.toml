[package]
name = "mailiner-html"
version = "0.1.0"
edition = "2021"
description = "Shared HTML/CSS sanitization policies for Mailiner viewer and composer"

[dependencies]
ammonia = "4.0"
regex = "1"
once_cell = "1.19"
