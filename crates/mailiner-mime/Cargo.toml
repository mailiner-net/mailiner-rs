[package]
name = "mailiner-mime"
version = "0.1.0"
edition = "2021"

[dependencies]
base64 = "0.22"
encoding_rs = "0.8"
thiserror = "2.0"
chrono = { version = "0.4", features = ["serde"] }
mailiner-core = { path = "../mailiner-core" }

[dev-dependencies]
serde_json = "1.0"
